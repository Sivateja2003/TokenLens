# TokenLens

A monorepo containing the Frontend and Backend for the TokenLens application.

## Structure

- `Frontend/` — React + Vite UI (Gemma E4B UI)
- `Backend/` — Python FastAPI backend with memory, RAG, and vector store
